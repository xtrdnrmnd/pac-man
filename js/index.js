// Using the MVC model to build the structure

// The controller is used for event processing
var controller = new Controller();

// The model is used for handling the Ajax and data processing
var model = new Model();

// The view interacts with DOM
var view = new View();

